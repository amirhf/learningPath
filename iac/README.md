# IaC (Terraform)

Modules to define: VPC, ALB, ECS (gateway, rag, planner, quiz, worker), RDS (Postgres), S3 (snippets, snapshots), SQS (ingest, replan), Cognito, Secrets Manager, X-Ray/CloudWatch setup.
Remote state: S3 + DynamoDB lock.
