# Phase 0 placeholder
FROM golang:1.22-alpine AS build
WORKDIR /src
# COPY go.mod go.sum ./
# RUN go mod download
# COPY . .
# RUN go build -o /bin/gateway
FROM alpine:3.20
# COPY --from=build /bin/gateway /bin/gateway
CMD ["sh", "-c", "echo Gateway Dockerfile placeholder"]
